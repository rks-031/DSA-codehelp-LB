#include <bits/stdc++.h>
using namespace std;

int main()
{
        // O(1)
        // in worst case, it can go upto: O(n)
        unordered_set<int> us;
        // set will have unique values but order can be random
        // all operations are similar to set but lower_bound and upper_bound do not work.
        
}