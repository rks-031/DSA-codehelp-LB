#include <bits/stdc++.h>
using namespace std;

int main()
{
        char name[] = "Babbar";
        int length = 0;
        for (int i = 0; name[i] != '\0'; i++)
        {
                length++;
        }
        cout << length;
}