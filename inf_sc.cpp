#include <bits/stdc++.h>
using namespace std;

int main()
{
        int num = 2;
        while (true)
        {
                switch (num)
                {
                case 1:
                        cout << "one";
                        break;
                case 2:
                        cout << "two";
                        break;
                }
                exit(0);
        }
}